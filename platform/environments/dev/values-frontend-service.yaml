# Value overrides for frontend-service in dev
replicaCount: 1

image:
  repository: frontend-service
  tag: dev-latest

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: "500m"
    memory: "512Mi"

ingress:
  enabled: true
  hosts:
    - host: frontend.dev.example.com
      paths:
        - path: /
          pathType: Prefix

env:
  LOG_LEVEL: "INFO"

global:
  labels:
    app.kubernetes.io/environment: dev

monitoring:
  enabled: false

nodeSelector: {}
affinity: {}
tolerations: []
