apiVersion: v1
kind: ConfigMap
metadata:
  name: notification-service-config
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "notification-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
  NODE_NAME: {{ include "notification-service.fullname" . | quote }}
